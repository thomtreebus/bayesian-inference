import {
  Node,
  Network,
  CptWithParents,
  CptWithoutParents,
  Infer,
  Combinations,
} from "../types";

export const infer: Infer = (
  network: Network,
  nodes: Combinations = {},
  sampleSize: number,
  observedValues?: Combinations
) => {
  return 1;
};
